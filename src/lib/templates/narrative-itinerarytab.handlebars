<div class="narrative-tab">
<!--    OPTION 1 9min/$2.50-->
    <span class="tab-part option-tab-part">
        <span class="option-label">OPTION</span> <span class="option-alpha">{{alphaValue}}</span>
    </span>
    <span class="tab-part duration-tab-part">
        {{formatDuration duration}}/{{fareString fare}}
    </span>
    <span class="tab-part modes-tab-part">
        {{#if hasWalkingLegs}}
            <div class="otp-legMode-icon otp-legMode-icon-WALK"></div>
        {{/if}}
    	{{#if hasBusLegs}}
            <div class="otp-legMode-icon otp-legMode-icon-BUS"></div>
        {{/if}}

    </span>
</div>

<script>
    $(".localize").localize();
</script>
